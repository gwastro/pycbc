#!/bin/env python
import argparse
from flask import Flask, render_template, url_for

parser = argparse.ArgumentParser()
parser.add_argument('--plots-base-dir')
args = parser.parse_args()

app = Flask('pycbc.results')

@app.route('/')
def base():
    tp = render_template('base.html', headings=['Example', 'another'], sections=['www.google.com', 'news.google.com'], zip=zip)
    return tp

app.run(debug=True)
